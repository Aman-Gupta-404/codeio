apiVersion: v1
kind: Service
metadata:
  name: codeio-frontend
  namespace: codeio

spec:
  selector:
    app: codeio-frontend

  ports:
    - name: http
      port: 3000
      targetPort: 3000

  type: ClusterIP
